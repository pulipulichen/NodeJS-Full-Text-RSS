const cPTTRemoveMetaline = function ($) {
  $('.article-metaline').remove()
  $('.article-metaline-tag').remove()
  $('.article-metaline-value').remove()
  $('.article-metaline-right').remove()
}

module.exports = cPTTRemoveMetaline